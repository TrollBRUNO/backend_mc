export class BindCardDto {
  card_id: string;
  // Зал, выдавший карту. Город берётся с него, клиент его не присылает:
  // раньше приходила локализованная строка и один зал попадал в базу
  // пятью разными названиями
  casino_id: string;
}
